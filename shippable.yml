language: ruby
cache: bundler
archive: true
rvm:
  - 2.1.2
script:
  - cd spec/dummy
  - bundle exec rake db:migrate
  - cd ../..
  - CODECLIMATE_REPO_TOKEN=123 bundle exec rspec
notifications:
   email: false
